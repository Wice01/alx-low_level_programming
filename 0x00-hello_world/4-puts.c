#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always zero (Success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
